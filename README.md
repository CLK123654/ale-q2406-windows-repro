# Q2406 Windows reproduction

This public repository contains one PostgreSQL Client task and its Windows Server 2025 workflow.

The workflow generates the Reference with PostgreSQL17, then checks the four final attachments in two clean directories. Formal execution uses only loopback database connections.

